[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4.13.0"
jlrs = {version = "0.22", features = ["async-rt", "tokio-rt"]}
serde = { version = "1.0.228", features = ["derive"] }
# common = { path = "../common" }
